# 🧵 Multithreading

This folder contains my Java **Multithreading** concept-based programs and practice code.

## 📚 Programs

| # | Program       | Concept Used                      |
| - | ------------- | --------------------------------- |
| 1 | FirstProgramm | Extending `Thread` Class          |
| 2 | RunnalbleDemo | Implementing `Runnable` Interface |
| 3 | MyRunnable    | `run()` Method & Runnable Task    |

## 🧠 Concepts Covered

* Multithreading
* `Thread` Class
* `Runnable` Interface
* `run()` Method
* `start()` Method
* Creating a Thread using `Thread`
* Creating a Thread using `Runnable`
* `Thread.currentThread().getName()`
* Multiple Threads
* Sequential vs Concurrent Execution
* Difference between `run()` and `start()`

## 📂 Structure

```text
Multithreading/
│
├── FirstProgramm.java
├── RunnalbleDemo.java
└── MyRunnable.java
```

## 🎯 Goal

To understand how **threads are created and executed in Java**, the difference between `Thread` and `Runnable`, and why `start()` is used to begin thread execution.

> **Create Task → Create Thread → start() → run() → Concurrent Execution** 🧵
